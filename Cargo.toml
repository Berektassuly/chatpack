[package]
name = "chatpack"
version = "1.0.0"
edition = "2021"
description = "Compress chat exports from Telegram, WhatsApp, and Instagram into token-efficient CSV for LLMs"
authors = ["Berektassuly"]
license = "MIT"
repository = "https://github.com/berektassuly/chatpack"
keywords = ["telegram", "whatsapp", "instagram", "llm", "csv"]
categories = ["command-line-utilities", "text-processing"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
csv = "1.3"

# Future dependencies for WhatsApp parsing:
# regex = "1.10"

[profile.release]
opt-level = 3
lto = true
strip = true
